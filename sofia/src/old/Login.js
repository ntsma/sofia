import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  header: {
    flexDirection: "row",
    marginBottom: 100
  },

  logo: {
    width: 100,
    height: 100
  },

  text: {
    color: "black",
    marginTop: 20,
    width: 200,
    textAlign: "center",
    opacity: 0.8,
    fontSize: 50
  }
});

export default styles;
